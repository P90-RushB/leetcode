class Solution:
    def findLongestWord(self, s: str, d: List[str]) -> str:

        def have_or_not(tmp_str):
            nonlocal s
                    idx += 1

                    res = i
                        elif ord(m) > ord(n):
